#include <bits/stdc++.h>

using namespace std;

int main() {
    int t, n, ni;
    cin >> t;
    for(int i = 0; i < t*2; i++) {
        int a[n];
        for(int j = 0; j < n; j++) {
            cin >> a[j];
        }
        sort(a, a+n);

    }
    return 0;
}
