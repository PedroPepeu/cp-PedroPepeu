#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ll n, m, k, l, r, d;
    cin >> n >> m >> k;
    ll iA[n];
    for(int i = 0; i < n; i++) {
        cin >> iA[i];
    }
    for(int i = 0; i < m; i++) {
        cin >> l >> r >> d;

        
    }
}
