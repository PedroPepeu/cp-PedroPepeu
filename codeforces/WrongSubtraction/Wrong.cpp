#include <iostream>
#include <bitset>

using namespace std;

int n, k;

int main() {
    cin >> n;
    cin >> k;
    while(k--) {
        bitset<8> b(n);
        if() {
            n--;
        } else {
            n /= 10;
        }
    }
    cout << n << endl;
}
