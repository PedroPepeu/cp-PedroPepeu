#include <iostream>

using namespace std;

int main() {
    int H, P, F, d;
    cin >> H >> P >> F >> d;

    if(d == -1) {
        if(H > F) {
            // cop < F and > heli
        } else {
            
        }
    } else {
        if(H > F) {

        } else {

        }
    }

    return 0;
}
