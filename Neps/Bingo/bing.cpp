#include <iostream>
#include <stdlib.h>

using namespace std;

int main() {
    int b, n;
    int a[b];
    while(cin >> n >> b && (n != 0 && b != 0)) {
        for(int i = 0; i < b; i++) {
            cin >> a[i];
        }

    }
}
