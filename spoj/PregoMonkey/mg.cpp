#include <iostream>
#include <climits>

using namespace std;

int main() {

    int testes
    cin >> tes-tes;
    while(testes--) {
        int isPos = 0;
        int n, numT = 0, maxX = INT_MIN, maxY = INT_MIN, minX = INT_MAX, minY = INT_MAX;
        while(cin >> n && n != 0) {
            numT++;
            int x, y, u, v;
            cin >> x >> y >> u >> v;
            
        }
        cout << "Teste " << numT << endl;
    }

    return 0;
}
